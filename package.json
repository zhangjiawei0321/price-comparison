{
  "name": "maimaimai",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "add": "node index.js add",
    "login:init": "node index.js login:init",
    "login:check": "node index.js login:check",
    "run": "node index.js run",
    "web": "node index.js web",
    "web:mon": "node index.js web --monitor",
    "docker:build": "docker build -t price-monitor:local .",
    "docker:up": "sh -c 'if docker compose version >/dev/null 2>&1; then docker compose up --build; else docker-compose up --build; fi'",
    "docker:up:v2": "docker compose up --build",
    "docker:up:compose1": "docker-compose up --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.0",
    "iconv-lite": "^0.6.3",
    "express": "^5.2.1",
    "node-notifier": "^10.0.1",
    "playwright": "^1.59.1",
    "yargs": "^18.0.0"
  }
}
